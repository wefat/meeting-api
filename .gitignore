# ------------------------------------------------------------------------------
# 1. Dependencies (ห้ามเอาโฟลเดอร์ไลบรารีขึ้นเด็ดขาด)
# ------------------------------------------------------------------------------
node_modules/
jspm_packages/
web_modules/
.pnp
.pnp.*

# ------------------------------------------------------------------------------
# 2. Environment Variables & Secrets (สำคัญมาก! ป้องกันข้อมูลรั่วไหล)
# ------------------------------------------------------------------------------
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key
*.cert

# ------------------------------------------------------------------------------
# 3. Build & Output (ไฟล์ที่ได้จากการ Compile/Build)
# ------------------------------------------------------------------------------
dist/
build/
out/
*.tsbuildinfo

# ------------------------------------------------------------------------------
# 4. Logs & Debugging (ไฟล์บันทึกประวัติการทำงานและข้อผิดพลาด)
# ------------------------------------------------------------------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ------------------------------------------------------------------------------
# 5. Database & Prisma (ถ้ามีการใช้งาน ORM / Local DB)
# ------------------------------------------------------------------------------
# ซ่อนไฟล์ฐานข้อมูล SQLite ที่สร้างขึ้นมาเล่นในเครื่อง local
*.db
*.db-journal

# ซ่อนไฟล์จาก Prisma ในกรณีที่มีการดึงข้อมูล Schema มาพักไว้
/prisma/migrations-backups

# ------------------------------------------------------------------------------
# 6. OS & IDEs (ไฟล์เฉพาะตัวของระบบปฏิบัติการและโปรแกรมเขียนโค้ด)
# ------------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor config ยอดนิยม
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?